# Playwright's postinstall downloads ~150MB of browsers. That is correct on a
# developer machine and pure waste on a build server, which never runs the
# tests — it burns install time and, on a constrained builder, can fail the
# deploy outright. Tests still run in CI and locally, where the browsers are
# fetched deliberately.
playwright_skip_browser_download=1
fund=false
audit=false
